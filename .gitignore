# ── Client / engagement data — NEVER commit (confidential + live credentials) ──
engagements/
brain/targets/
brain/ruflo-export.jsonl
.t3mp3st/
**/.t3mp3st/

# ── Credentials / tokens / secrets ──
**/.token*
**/*.pem
**/*.key
**/.env
**/.env.*

# ── OS / scratch ──
.DS_Store
*.bak
.opt-hashes.tmp
/tmp/

# CDC harness per-target working state
.cdc/

# Externally-cloned packs (provenance + re-hydrate recipe in packs/UPSTREAM.md,
# scripts/reinstall-packs.sh). Nested .git dirs — never commit into this repo.
packs/xalgorix/
packs/dalfox/
packs/rifteo-skills/

# Asset registry — regenerable from source via scripts/build-registry.py.
# Canonical remains the file tree; this is the machine-readable index.
brain/registry/
