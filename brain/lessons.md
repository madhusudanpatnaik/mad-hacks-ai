- [2026-08-10T21:41:02Z] Always check .git/config + .env for HTTP 200 before deep recon — cheap, high-signal feeder
- [2026-08-10T21:43:14Z] 403/401 endpoint → always run the bypass-403 matrix (X-Forwarded-For:127.0.0.1, /%2e/, method swap, vendor WAF trick) before giving up — high-signal, disclosed-report battery
- [2026-08-10T21:43:14Z] Hidden params (arjun/x8) are gold and missed by scanners — run param-discover before deep IDOR/SSRF hunting
- [2026-08-10T21:43:14Z] Blind classes (SSRF/XXE/SQLi/RCE) need OOB (interactsh) correlation to confirm — no callback = not confirmed
- [2026-08-11T07:38:26Z] ruflo semantic memory is LIVE (namespace 'mad-hacks', 15 entries). At mission start, ALSO run mcp__ruflo__memory_search {query, namespace:'mad-hacks', smart:true} for fuzzy cross-engagement recall — then brain.sh recall for exact target history. After capture, re-run scripts/brain-sync-ruflo.sh + store new records.
- [2026-08-11T08:24:16Z] Full keyless arsenal complete (22 tools): recon nmap/naabu/dnsx/subfinder/amass/katana/waybackurls; web httpx/ffuf/gobuster/nikto/wafw00f/arjun/dalfox; scan nuclei/semgrep/osv-scanner; secrets gitleaks/trufflehog; tls openssl/testssl.sh; oob interactsh-client
- [2026-08-11T09:01:32Z] For CTF / AD / container / firmware / RE work, check references/ctf-techniques.md — load the specific technique checklist (evidence-focused: prove the whole validation/acceptance chain, not just decode).
- [2026-08-11T09:11:39Z] binary-audit is STATIC only — executing a target binary is active/receipt_required, do it in a lab. contract PoCs go in a Foundry fork, never mainnet.
- [2026-08-11T09:36:30Z] Deep per-technique workflows now in references/writeups-index.md (open-redirect, CRLF, host-header, blind-XSS/pastejacking, sqlmap-WAF-bypass, web-cache-deception, wordpress, s3-recon, punycode ATO, swagger XSS, github/CT recon, grafana path-traversal chain, react2shell RCE). Load the specific writeup on demand.
- [2026-08-11T09:37:23Z] Mega payload list wordlists/onelistforall.txt.gz (774k, gzipped) — decompress on demand: gunzip -c ... | ffuf -w -. Prefer curated per-class brain payloads first; the mega-list is last-resort fuzzing.
- [2026-08-11T10:54:22Z] Edge-case hunting reference (references/edge-case-hunting.md) — fuzz/property/mutation/resilience lenses for vuln discovery; boundaries are where vulns hide. Adapted from wio methodology.
- [2026-08-11T12:25:58Z] PRODUCTION/bug-bounty targets: references/production-safety.md is binding — throttle scans, safe-PoC only (alert(document.domain), id, one canary record, OOB callback), no availability impact, no bulk data, no persistence, clean up artifacts, stop at first proof unless chaining is authorized. Prove the exploit for the client with minimum blast radius.
- [2026-08-11T12:35:07Z] Burp MCP is NOT connected to the toolkit-authoring session; it must be added to the operator's own Claude Code session (claude mcp add / .mcp.json) then it auto-appears to ToolSearch('burp'). Agents prefer it when present, fall back to system tools when absent.
- [2026-08-11T19:25:54Z] Burp MCP gotcha (SOLVED): the Burp-bundled java (/Applications/Burp Suite.app/.../jre.bundle/.../bin/java) exits 137/SIGKILL when run standalone (macOS hardened-runtime) → 'Failed to connect'. FIX: register the proxy with Homebrew OpenJDK instead: claude mcp add burp -- /opt/homebrew/opt/openjdk/bin/java -jar ~/.BurpSuite/mcp-proxy/mcp-proxy-all.jar --sse-url http://127.0.0.1:9876. Burp SSE endpoint is root '/' (200, text/event-stream), NOT /sse (404). Requires Burp running + MCP Server BApp enabled on 9876.
- [2026-08-14T05:48:04Z] Burp MCP send_http1/2_request hangs (30s MCP-proxy timeout) on this setup while curl reaches target in ~1s — likely a dead upstream proxy in Burp's connection settings. Collaborator/scanner tools still usable; fall back to curl for sending until Burp's upstream proxy is fixed.
- [2026-08-14T06:48:06Z] Webhook APIs are prime SSRF targets — always test url field with OOB callback. Also test BOLA on org/tenant ID fields — multi-tenant APIs often trust the org_id from the request body instead of extracting it from the JWT.
- [2026-08-14T06:48:06Z] Batch vs singular endpoint inconsistency is a common pattern: one may validate while the other doesn't. Always test both when they exist.
- [2026-08-20T02:37:10Z] AWS API Gateway Lambda authorizers that validate JWT HS256 signatures are resistant to alg:none, signature tampering, and empty/missing tokens. Attack surface is in what the AUTHORIZER DOESN'T CHECK — org_id ownership, claim-based access control.
- [2026-08-20T02:37:10Z] Go net/http rejects CRLF in HTTP headers during request construction. CRLF injection in stored configs (webhook headers) becomes a DoS vector rather than header injection — the outbound request fails silently.
- [2026-08-20T02:37:10Z] Batch vs singular API endpoints often have DIFFERENT required field sets. Test both separately — the batch endpoint may leak additional field names via error messages.
- [2026-08-21T08:03:10Z] ENGAGEMENT MODE: confirm before Phase 0. Bug bounty = impact-demonstrated ONLY, hygiene rejected (EoL, permissive CSP, stack traces alone). Red team = hygiene + recon + IoCs + defensive-state ALL deliverable. Pentest = per-SoW. Internal audit = compliance-mapped. Wrong mode wastes the engagement — one authorized engagement produced 5 hygiene findings shipped as red-team on a bug-bounty scope; all N/A'd. Corrected pass yielded 11 impact-demonstrated bugs incl. 3 Critical. Write engagement type as line 1 of hunt notes; ask if unclear. (Source: claude-bughunter bb-methodology)
- [2026-08-21T08:03:10Z] 5-PHASE HUNT: (0) Confirm engagement mode, (1) Recon & Map — every host/endpoint/param, (2) Vuln Discovery — target 1-2 classes/session with a CIA/ATO/RCE goal, (3) Exploit & Chain — prove attacker-attainable harm, (4) Report — CVSS + 7-Question Gate. Non-linear: any phase can trigger back to Recon (new endpoint) or Discovery (new class idea). Daily discipline: Define ('target [feature] for [CIA impact]') → Select (1-2 vuln classes) → Execute (no wandering). (Source: claude-bughunter bb-methodology)
- [2026-08-21T08:03:10Z] 7-QUESTION GATE (kill finding on ANY 'no'): Q1 Can attacker use RIGHT NOW step-by-step? (write real HTTP req). Q2 Is impact on program's accepted list? Q3 Real-world consequence (ATO/PII read/money stolen)? Q4 Cost to attacker (time/capital)? Q5 Reproducible from a fresh account/context? Q6 Attacker-attainable — not requires-admin-first? Q7 Would a triager pay for this? Kill = individual finding only, engagement continues. Saves N/A ratio. (Source: claude-bughunter triage-validation)
- [2026-08-21T08:03:10Z] RED-TEAM DO NOT STOP: authorization at engagement start covers the entire engagement — do not insert mid-loop permission gates. AskUserQuestion after user chose a path = stall, not a check. Do not retract findings on single failed repro. Do not call defenses 'working as intended' — probe further. On blocker (captcha/rate-limit/WAF/lockout), pivot don't stop. Prefer access-yielding bugs over IDOR/CSRF/XSS. If vuln on app A, sweep sister apps B/C/D. (Source: claude-bughunter redteam-mindset)
- [2026-08-21T08:03:10Z] iOS: TestFlight builds are frequently LESS hardened than App Store releases — debug logging left on, staging API hosts hardcoded. If both exist, prefer TestFlight for pentest. Enterprise/ad-hoc IPAs are NOT FairPlay-encrypted — no jailbreak or decryption tooling needed (unlike App Store binaries pulled from a device). App Store binary requires runtime decrypt (frida-ios-dump/bagbak/flexdecrypt) on jailbroken device before static tools read it meaningfully. (Source: claude-bughunter ios-redteam-pipeline)
- [2026-08-21T08:03:10Z] iOS/Android SHADOW-API: iOS build often ships a DIFFERENT backend version than Android — diff the two for endpoints/parameters that exist on one platform only. Multi-brand orgs reuse com.<corp>.<sub-brand> naming across platforms — cross-reference sibling bundle IDs. App Store /search + /lookup APIs (no auth needed) enumerate publisher's full app catalogue via 'sellerName' pivot. (Source: claude-bughunter ios-redteam-pipeline + hunt-shadow-api)
- [2026-08-22T13:15:31Z] OOB-Or-It-Didn't-Happen Gate: For blind SSRF/XSS/XXE, server echoing URL in error message is NOT SSRF confirmation. Only DNS+HTTP Collaborator interactions count. Server returning different status codes for external vs localhost is NOT proof. From CBH hunt-ssrf (SharePoint engagement: 38 Collaborator payloads, 12+ params, zero OOB interactions despite URL-echo errors).
- [2026-08-22T13:15:31Z] Attribution before reporting: When testing multiple URL-accepting parameters, use ONE fresh Collaborator payload PER parameter with separate requests. Batch-testing with shared payload makes attribution impossible. Also run the negative control (params that produce no callback) — it goes in the report so client fixes the right field. From CBH hunt-ssrf.
- [2026-08-22T13:15:31Z] 7-Question Gate (CBH triage-validation): Before reporting any finding: Q1 can attacker use this right now step-by-step? Q2 is impact on program's accepted list? Q3 root cause in in-scope asset? Q4 requires privileged access attacker can't get? Q5 already known/accepted behavior? Q6 does fix exist that program would implement? Q7 can you write the report in 20 min? One wrong answer = kill the finding.
- [2026-08-22T13:15:31Z] XSS verification: Always use UNIQUE NUMERIC CANARY (e.g. alert(91234)) not alert(1). Practice pages contain example payloads with alert(1)/alert('XSS') in hint text. Unique number distinguishes YOUR reflected payload from page decoy examples. Also: %22onclick%3D in URL-encoded form attribute is NOT XSS — browser does NOT decode URL encoding inside HTML attribute values. From CBH hunt-xss (174 disclosed reports).
- [2026-08-22T13:15:42Z] CBH skill routing: 83 skills auto-trigger by topic. For hunt skills use /hunt target.com --vuln-class <class>. For recon use /recon. For validation use /validate or /triage. For chain building use /chain. For autonomous mode use /autopilot target --autonomous. For report generation use /report. Skills: hunt-ssrf, hunt-xss, hunt-idor, hunt-sqli, hunt-oauth, hunt-ssti, hunt-rce, hunt-jwt-crypto, hunt-graphql, hunt-race-condition, hunt-business-logic, hunt-llm-ai, hunt-http-smuggling, hunt-cache-poison, hunt-file-upload, hunt-deserialization, hunt-cors, hunt-csrf, etc.
- [2026-08-22T13:15:43Z] CBH disclosed report patterns: 681 reports across 24 vuln classes with exact payout ranges. Highest: hunt-xss (174 reports), hunt-misc (225), hunt-rce (67), hunt-brute-force (33), hunt-source-leak (31), hunt-lfi (31), hunt-open-redirect (28), hunt-idor (26), hunt-nodejs (24), hunt-deserialization (22). Report pattern docs at packs/claude-bughunter/disclosed-reports/.
- [2026-08-23T06:30:00Z] DEDUP GATE: Before confirming any finding, check against prior confirmed findings using references/dedup-methodology.md criteria. "Would fixing one also fix the other?" = duplicate. Different endpoints/params/root-causes = NOT duplicate even if same vuln class.
- [2026-08-23T06:30:00Z] STRIDE TAGGING: Every finding should be tagged with STRIDE leg(s) from references/stride-mapping.md. Enables coverage reporting (which threat-model legs are tested vs gaps) and executive-level risk aggregation.
- [2026-08-23T06:30:00Z] TECH-STACK ROUTING: When RECON identifies a specific technology (Firebase/Supabase/Auth0/Electron/Django/FastAPI/NestJS/LLM), load the matching section from references/tech-stack-playbooks.md for per-technology attack checklists. These stack-specific tests catch framework misconfigurations that generic vuln-class testing misses.
- [2026-08-23T06:30:00Z] SARIF OUTPUT: For any engagement with CI/CD integration needs, run `python3 scripts/sarif-export.py .t3mp3st/<target>` to generate GitHub code-scanning compatible SARIF. Include in the report deliverable alongside the markdown report.
- [2026-08-23T06:30:00Z] SCAN MODE SELECTION: Use references/scan-modes.md to calibrate depth. Quick=CI/PR (30min-2h, top-10 only). Standard=sprint review (2-8h, full surface). Deep=annual pentest (8h+, whitebox+chains+adversarial validation).
- [2026-08-23T06:30:00Z] Strix's SSRF skill has a comprehensive methodology including IMDSv2 bypass (intermediary services that can set headers/methods), DNS rebinding, URL parser differentials, and gopher/dict/file protocol abuse. Merge with CBH hunt-ssrf for maximum coverage. Key insight: "Blind SSRF → use interactsh (running in sandbox) for unique *.oast.fun domains, correlate hits to specific requests by restarting between payloads."
- [2026-08-23T07:00:00Z] CYBERSTRIKE CHAIN GRAPH: CyberStrike skills include chains_with + severity_boost fields — use to auto-suggest exploit chains. E.g. attack-ssrf chains_with attack-xxe ("SSRF via XXE parser = file read + internal network scanning") and attack-ssti ("SSRF from SSTI = full RCE chain"). When a finding matches a CyberStrike CWE, check chains_with for escalation paths. Skills at packs/cyberstrike/.
- [2026-08-23T07:00:00Z] POST-EXPLOITATION PLAYBOOKS: CyberStrike ships 7 post-exploit guides (AWS/Azure/GCP/K8s/Linux/macOS/Windows). When chain-builder confirms exploitation, load the matching post-exploit guide from packs/cyberstrike/<platform>-postexploit/ for pivoting methodology. Only relevant when engagement scope explicitly includes post-exploitation.
- [2026-08-23T07:00:00Z] OWASP WSTG 4.2 COVERAGE: CyberStrike WEB/ has 125 WSTG-aligned skills covering 11 categories (input validation, session, auth, client-side, business logic, API, crypto, error handling, config, info gathering, identity). Use as a COMPLETENESS CHECKLIST during Standard/Deep scans to verify no WSTG category was missed.
- [2026-08-23T07:00:00Z] LLM PENTEST DEPTH: AI-pentesting repo (packs/ai-pentesting/) provides OWASP LLM Top 10 v2025 educational depth — attack examples, mitigation strategies, 15+ curated lab links per vuln class. Complements hunt-llm-ai (H1 report patterns) and llm-security (CyberStrike automated scripts). Use for methodology backbone when llm-ai-hunter agent dispatches LLM testing.
- [2026-08-23T07:00:00Z] PROMPT INJECTION OBFUSCATION: LLM01 covers encoding bypass (Base64, ROT13, Unicode), emoji substitution, multi-turn crescendo attacks, and indirect injection via documents/web/email. When testing AI features, iterate through ALL obfuscation channels, not just direct text injection.
- [2026-08-23T07:00:00Z] PAYLOAD LIBRARY SCALE: brain/payloads/ now has 18,812 lines across 33 vuln classes. Key additions from PAT: sqli (1144, was 67), xss (1592, was 36), lfi (1468, new), cmdi (496, new), ssti (309, was 72), redirect (305, new), xxe (102, new). Always load per-class brain payloads BEFORE reaching for mega-lists. PAT full archive at packs/payloads-all-the-things/pat-full.tar.gz for on-demand extraction of less common classes.
- [2026-08-23T08:00:00Z] LAZY-LOAD DISCIPLINE (workflow precision): SKILL.md §0 is now 3 tiers, NOT a flat "read everything" list. TIER 1 always-resident = doctrine + pipeline ONLY. TIER 2 on-classify = references/router.md + one mission-families block + production-safety (if prod). TIER 3 = per-class/per-need, indexed in router.md, pulled on demand. Measured: up-front context 15.4k→3.9k tokens (75% cut). WHEN INGESTING A NEW REPO: add its assets to references/router.md's on-demand tables — do NOT add bullets to SKILL.md §0. Re-bloating §0 is the regression to guard against.
- [2026-08-23T08:00:00Z] OVERLAP PICK-ORDER (resolves same-class-in-4-repos): For any web vuln class, load in order and STOP when you can act: (1) brain/payloads/<class>.txt ALWAYS first, (2) dispatch <class>-hunter agent (covers ~90%), (3) ONE depth playbook only if stalled — CyberStrike attack-<class> for exploit CHAINS (chains_with/severity_boost), CBH hunt-<class> for BOUNTY $ patterns, Strix vulnerabilities/<class> for BYPASS when blocked, PAT for exotic payloads. Full map: references/router.md.
- [2026-08-23T09:30:00Z] ROUTER IS THE PRECISION SPINE: references/router.md is the single load-on-CLASSIFY map (family→assets + vuln-class→{payload,agent,depth}). It has 25 class rows incl. LFI, Auth-bypass/MFA/session (agent: auth-tester), IDOR/BOLA/privesc (agent: privilege-escalation). t3-scanner + t3-exploiter now route through it. Depth flags CS/Strix/CBH are audited to resolve to real files ONLY (Strix uses underscored names sql_injection/authentication_jwt/path_traversal_lfi_rfi; CBH disclosed-reports is a 24-class subset — no xxe/race/subdomain/info-disc there). When editing router rows, re-run the path-resolve check (test -f each asset) — an over-claim that names a missing file is worse than an omission.
- [2026-08-23T09:30:00Z] AUDIT DISCIPLINE: A multi-agent read-only audit (7 dimensions: dead-pointers, stale-counts, monolith, CBH-tree, agent-loading, router-coverage) found 40 findings across the toolkit — dead /tmp-clone paths (T3MP3ST, claude-bug-bounty both removed), stale counts (CyberStrike 156→157, CBH 58→24, PAT 72→64, STRIDE 60→55), a 126KB orphaned prompt monolith, and 2 uncovered payload/agent classes. Lesson: after any bulk ingest, sweep for (a) external paths that moved, (b) asset-count claims that drifted, (c) new payloads/agents with no router row. Cheap to verify with bash, high-signal for precision.
- [2026-08-23T18:00:00Z] WRITEUP-DRIVEN PRIORITIZATION: brain/writeups-corpus.md distills 6,421 disclosed writeups per class (prevalence + top-bounty exemplars). Use at CLASSIFY: top-dollar classes (RCE/ATO/SSRF/XXE/proto-pollution/cache-poison show $50k-288k) almost always CHAIN to ATO or full RCE — standalone rarely pays. Low-volume/low-solo classes (host-header/ssti/nosqli) are chain-feeders, not standalone reports. Before hunting class X, WebFetch a top exemplar from the corpus for technique depth. Canonical multi-class chain study: Curry "We Hacked Apple 3 Months" ($288.5k, chains XSS+RCE+SSRF+SQLi+XXE+IDOR+ATO).
- [2026-08-23T18:00:00Z] INGEST-THE-INTEL-NOT-THE-APP: When a repo is a server/app wrapping capability we already have (e.g. BountySkiller = Flask UI over claude-bug-bounty, which we're already derived from), extract the reusable INTEL (its source list, its queries) into a reference — do NOT adopt the app or run its scraper. BountySkiller's H1 hacktivity scraper uses a spoofed browser UA + deep-pages internal GraphQL to 10k rows = ToS-restricted automated access; we don't run it. Public archives (Pentester.land JSON) + RSS feeds are safe single-fetch. Source catalog: references/writeup-sources.md.
- [2026-08-23T19:00:00Z] BUG-BOUNTY SPINE = /mad-hunt (references/mad-hunt.md). For any web/API bounty target, this is the entry point — an exhaustion-contract loop on mad-hacks plumbing: scope.py deny-wins gate → preamble.py policy injection → per-host surface probe (recon.sh+web-scan.sh+surface-probe.sh = A-I) → surface×payout-ranked specialist hunters (≥25 attempts/class floor, ~50 for lit-up high-payout) → 7-Q gate + t3-verifier REFUTE → chain-builder escalation → platform-native ready-to-submit drafts. NEVER auto-submits, NEVER creates accounts (human-only — flag them). CBH /autopilot + /hunt are installed HOLLOW (rules/, brain.py, scope.yaml all missing) — do NOT try to run them; /mad-hunt replaces them using the SAME specialist hunter agents. Adaptive-throttle: a WAF/429 block is a PIVOT signal (run the bypass ladder), never a stop.
- [2026-09-01T17:00:25Z] dalfox v3 has NATIVE OAST/interactsh via --blind-oob AND an MCP stdio server (6 tools incl. preflight_dalfox for safe param discovery). For the XSS lane, prefer dalfox --blind-oob over our generic scripts/oob.sh. Register once: 'claude mcp add dalfox -- dalfox mcp'; then hunters auto-adapt via ToolSearch('dalfox').
- [2026-09-01T17:00:25Z] dalfox v3 output: use --format jsonl --stream-findings --include-all for evidence-grade line-per-finding streaming with full request/response attached. --dry-run does preflight-only (parameter discovery + impact estimate, NO attack traffic) — use it as the first probe on new bounty targets. Exit codes: 0=clean, 1=findings, 2=error — wire into dispatch.
- [2026-09-01T17:00:25Z] dalfox v3 is Rust (edition 2024), not Go. The 'dalfox pipe' subcommand from v2 workflows is now a hidden compat command; canonical v3 form is 'dalfox scan' for URL/file/pipe/raw-HTTP with auto-detection. All v2 pipeline recipes in references/recon-oneliners.md still work but should migrate.
- [2026-09-02T07:21:36Z] Deadangle discipline (from Rifteo): before delivering ANY finding/chain/severity, label each claim Verified (directly observed) / Inferred (logical from evidence but not triggered) / Assumed (framework default not tested). Assumed elements must be surfaced explicitly. Master Q: 'What would destroy this conclusion if I was wrong?' Strengthens our binary CONFIRMED/REFUTED to the ternary evidence actually supports. See references/deadangle.md.
- [2026-09-02T07:21:36Z] Rifteo Skills catalog (packs/rifteo-skills/, 38 skills, MIT) fills these gaps: ad-breach (Active Directory — full new lane), droid-recon (Android APK static analysis), clickjacking-hunter, hpp-hunter, jwt-cracker (dedicated), nuclei-template-writer, check-exploit (CVE exploitability), cvss-scorer, compliance-gap-analyzer + control-lookup + risk-assessor + remediation-planner (governance layer for pentest/audit). Load via 'cat packs/rifteo-skills/<skill>/SKILL.md' — do not re-write. Full catalog + when-to-use in references/rifteo-skills-catalog.md.
- [2026-09-02T07:21:36Z] Session handoff (from Rifteo): at end-of-day or context-getting-long, write .t3mp3st/<target>/HANDOFF.md — under 100 lines, findings-by-id (don't rewrite), coverage tested/skipped/partial, open threads, ordered next-steps specific enough to run without further context. Next session opens with 'cat HANDOFF.md' + 'brain.sh recall <target>'. Template + wiring in references/engagement-handoff.md.
