# EXTRACTION — `t3mp3st`

- **Marker:** `.vendored` + `.internal` (parent framework, doctrine core, not a routed attack class)
- **License:** AGPL-3.0
- **First mined:** initial mad-hacks build (mad-hacks is derived from t3mp3st); normalized 2026-09-08 (marker-only)

## Rationale — why this pack is in the harness

t3mp3st is the **parent framework** mad-hacks was built on. Its doctrine (SCOPE + VERIFY + REFUTE gates, keyless operation, brain compounding across engagements) IS the mad-hacks doctrine — see `references/doctrine.md` for the current form. The pack is retained as historical context and license attribution (AGPL-3.0). Not a separate knowledge source; the derived doctrine lives natively in mad-hacks.

## Extracted lessons

None. All useful t3mp3st doctrine already lives in `references/doctrine.md` + `references/pipeline.md` as mad-hacks-native, not as atomic records pointing at the pack.

## Extracted patterns

None. Doctrine enforcement is via `scripts/scope.py` + `scripts/preamble.py` + `agents/operators/t3-verifier.md` (adversarial gate) — not as brain records.

## Extracted tools

None. t3mp3st runtime tools are re-implemented as mad-hacks shell scripts (`scripts/brain.sh`, `scripts/scope.sh`, `scripts/recon.sh`, etc.).

## Extracted payloads

None. Payload contributions from t3mp3st are folded into `brain/payloads/` — same story as CBH.

## Router integration

Marked `.internal` — the framework's doctrine + pipeline are always-resident, not routed as a lookup class.

## Notes / next re-mining

- License attribution is the primary reason the pack is retained. Do not delete without a license-compliance review.
